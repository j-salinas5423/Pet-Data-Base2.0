# Pet-Data-Base2.0
Updated version of previous repository "Pet Data Base"; this version features a main menu where the user can select their desired actions from adding a new pet into the database, editing the data or removing some information from the database.
